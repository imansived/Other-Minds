# --- serving path (kept lean; this is what the Docker image installs) ---
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
python-dotenv>=1.0

# LLM. Provider is swappable via config.PROVIDER; only the Gemini
# integration is installed by default. Add langchain-anthropic /
# langchain-openai here to enable those.
langchain-core>=0.3
langchain-google-genai>=2.0
